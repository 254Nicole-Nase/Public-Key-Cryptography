# Base, exponent, and modulus
base = 101
exponent = 17
modulus = 22663

# Using the built-in pow function for modular exponentiation
result = pow(base, exponent, modulus)

# Print the result
print(result)

